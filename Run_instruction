 run `npm audit fix` to fix them, or `npm audit` for details

  Run instructions for iOS:
    • cd "D:\On-Demand-Delivery-App\OnDemandDelivery" && npx react-native run-ios
    - or -
    • Open OnDemandDelivery\ios\OnDemandDelivery.xcodeproj in Xcode or run "xed -b ios"
    • Hit the Run button

  Run instructions for Android:
    • Have an Android emulator running (quickest way to get started), or a device connected.
    • cd "D:\On-Demand-Delivery-App\OnDemandDelivery" && npx react-native run-android
